<li>
	{{name}}

	<button class="change-eatstatus" data-id="{{id}}" data-newdevour="{{devour}}">
		{{#if devour}}DELETE{{else}}DEVOUR!{{/if}}
	</button>

	<button class="delete-burger" data-id="{{this.id}}">DELETE!</button>
</li>
